# PATH var
export PATH=$HOME/shell:/usr/local/share/npm/bin:/Users/jason/pear/bin:/usr/local/bin:$PATH

# Command Prompt
export PS1="\W $ "


#########################
# Custom Aliases
#########################
 
alias ..="cd .."
alias la="ls -la"

# Colorize the ls output
alias ls='ls --color=auto'

# Force mkdir to create intermediate directories by default
alias mkdir='mkdir -p'